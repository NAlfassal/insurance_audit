[project]
name = "insurance-auditing"
version = "0.1.0"
description = "Contract-vs-invoice auditing for five hospital service contracts"
requires-python = ">=3.11"

dependencies = [
    "pandas==2.2.3",
    "numpy==2.1.3",
    "python-dotenv==1.0.1",
    "google-genai==0.3.0",
]

[dependency-groups]
dev = ["pytest==8.3.3"]

[tool.uv]
package = false
