# Copy to .env and fill in. .env is gitignored and never committed.

# Optional. Only src/llm_crosscheck.py uses it; the pipeline itself needs no key.
GEMINI_API_KEY=

# Model used for the optional LLM cross-check of contract extraction.
GEMINI_MODEL=gemini-2.0-flash
